import { createData, insertData } from "./createData";

describe("Тестируем добавление записи", () => {
  it("Создаем переменные для добавления и проверяем что запись есть", () => {
    expect(1).toBe(1);
  });
});
