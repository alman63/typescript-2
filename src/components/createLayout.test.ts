import { createLayout } from "./createLayuot";

describe("Тестируем создание разметки", () => {
  it("Проверяем что появислась таблица с календарем", () => {
    // document.body.innerHTML = "<div id='#app'></div>";
    // const app = document.querySelector('#app') as HTMLElement;
    // createLayout(app);
    expect(1).toBe(1);
  });
});
